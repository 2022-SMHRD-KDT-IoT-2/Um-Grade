<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.smhrd.mapper.UserMapper">
	
	<!-- 유저 회원가입 -->
	<insert id = "userJoin" parameterType = "kr.smhrd.domain.User">
		insert into t_user (user_id, user_pw, user_nick, user_email, user_phone, user_addr)
		values (#{user_id}, #{user_pw}, #{user_nick}, #{user_email}, #{user_phone}, #{user_addr})
	</insert>
	<!-- 유저 로그인 -->
	<select id  = "userLogin" parameterType = "kr.smhrd.domain.User" resultType = "String">
		select user_pw from t_user where user_id = #{user_id} and user_pw = #{user_pw}
	</select>
	
</mapper>
